PROJECT_NAME="Unified Precision Agriculture Platform"
API_V1_STR="/api/v1"
SECRET_KEY="supersecretkey-change-in-production"
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# Database (PostgreSQL default, falls back to SQLite if PostgreSQL unavailable)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/agri_platform"
DATABASE_URL="sqlite:///./app.db"

# CORS
BACKEND_CORS_ORIGINS=["*"]

# Razorpay Test Mode
RAZORPAY_KEY_ID="rzp_test_mock"
RAZORPAY_KEY_SECRET="mock_secret"

# Required for authenticated physical-device REST ingestion.  Do not use the
# simulator or a physical device until this is replaced with a secret value.
CEA_DEVICE_API_KEY=""
